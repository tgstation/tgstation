language: generic
dist: xenial
sudo: false

branches:
  except:
  - ___TGS3TempBranch
  - ___TGSTempBranch

matrix:
  include:
    - name: "Run Linters"
      env: BUILD_TOOLS=true
      addons:
        apt:
          packages:
            - python3
            - python3-pip
            - python3-setuptools
      install:
        - tools/travis/install_build_tools.sh
      script:
        - tools/travis/check_filedirs.sh tgstation.dme
        - tools/travis/build_tools.sh
        - tools/travis/lint_grep.sh

    - name: "Compile All Maps"
      env: BUILD_TOOLS=false BUILD_TESTING=true
      addons:
        apt:
          packages:
            - libstdc++6:i386
      cache:
        directories:
          - $HOME/BYOND
      install:
        - tools/travis/install_byond.sh
      before_script:
        - tools/travis/template_dm_generator.py
      script:
        - tools/travis/build_byond.sh

    - name: "Compile and Run Tests"
      env: BUILD_TOOLS=false BUILD_TESTING=false
      addons:
        mariadb: '10.2'
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - libstdc++6:i386
            - libssl-dev:i386
            - gcc-multilib
            - g++-7
            - g++-7-multilib
            - libmariadb-client-lgpl-dev:i386
            - libmariadbd-dev
      cache:
        directories:
          - $HOME/.cargo
          - $HOME/BYOND
          - $HOME/MariaDB
          - $HOME/.rustup
      install:
        - tools/travis/install_byond.sh
      before_script:
        - mysql -u root -e 'CREATE DATABASE tg_travis;'
        - mysql -u root tg_travis < SQL/tgstation_schema.sql
        - mysql -u root -e 'CREATE DATABASE tg_travis_prefixed;'
        - mysql -u root tg_travis_prefixed < SQL/tgstation_schema_prefixed.sql
      script:
        - tools/travis/build_dependencies.sh || travis_terminate 1
        - tools/travis/build_byond.sh
