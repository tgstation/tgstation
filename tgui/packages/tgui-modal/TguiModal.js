import { Window } from 'tgui/layouts';
import { Button } from 'tgui/components';

export const TguiModal = () => {
  return (
    <Window>
      <Window.Content>
        <Button>Test</Button>
      </Window.Content>
    </Window>
  );
};
