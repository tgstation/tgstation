import { CheckboxInput, FeatureToggle } from '../base';

export const auto_fit_viewport: FeatureToggle = {
  name: 'Автоматической подгонка по размеру к экрану',
  category: 'Интерфейс',
  component: CheckboxInput,
};
