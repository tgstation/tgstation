import { Box, Stack } from '../../components';

export const Pin = (props) => {
  return (
    <Stack>
      <Stack.Item>
        <Box className="Evidence__Pin" textAlign="center" />
      </Stack.Item>
    </Stack>
  );
};
