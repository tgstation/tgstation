<ui-display>
  <ui-section label='Status'>
    <ui-button
          action='toggle_power'
          style='{{data.on ? "selected" : null}}' >
          Turn {{data.on ? "off" : "on"}}
    </ui-button>
  </ui-section>
</ui-display>