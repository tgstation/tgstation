/// From remove_depth_info() OR add_depth_info(): (list/depths, removing)
#define COMSIG_CELL_DEPTH_CHANGED "cell_depth_changed"
/// From remove_z_info() OR add_z_info(): (list/z_levels, removing)
#define COMSIG_CELL_Z_CHANGED "cell_z_changed"
