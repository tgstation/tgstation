/// Each overlay circuit component has it's own ID. We use these when we create alternate_appearence because they use id keys and we don't want them to delete each other when you make a new appearance from a different circuit
GLOBAL_VAR_INIT(object_overlay_id, 0)
