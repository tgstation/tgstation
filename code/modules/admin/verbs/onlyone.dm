/client/proc/only_one()
	if(!ticker || !ticker.mode)
		alert("The game hasn't started yet!")
		return
	return
	// TODO DATUM ANTAGS

/proc/only_me()
	if(!ticker || !ticker.mode)
		alert("The game hasn't started yet!")
		return
	// TODO DATUM ANTAGS
