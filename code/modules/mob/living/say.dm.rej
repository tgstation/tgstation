diff a/code/modules/mob/living/say.dm b/code/modules/mob/living/say.dm	(rejected hunks)
@@ -135,10 +135,12 @@ var/list/crit_allowed_modes = list(MODE_WHISPER,MODE_CHANGELING,MODE_ALIEN)
 	spans += get_spans()
 
 	if(language)
-		// TODO use preinstanced datums for the span lists
-		var/datum/language/L = new language
+		var/datum/language/L = language_datums[language]
+		if(!istype(L))
+			L = new language
+			language_datums[language] = L
+
 		spans |= L.spans
-		qdel(L)
 
 	//Log what we've said with an associated timestamp, using the list's len for safety/to prevent overwriting messages
 	log_message(message, INDIVIDUAL_SAY_LOG)
