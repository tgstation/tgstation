/// amount of alive people for highpop revolution threshold
#define REV_HIGHPOP_THRESHOLD 70
/// percent before ascend (lowpop)
#define REV_ASCENDENT_LOWPOP 0.5
/// percent before ascend (highpop)
#define REV_ASCENDENT_HIGHPOP 0.4
