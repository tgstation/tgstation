<article class='display'>
  {{#if title}}
    <header>
      <div style='float:left'>{{yield left}}</div>
      <h3>{{title}}</h3>
      <div style='float:right'>{{yield right}}</div>
    </header>
  {{/if}}
  {{yield}}
</article>
